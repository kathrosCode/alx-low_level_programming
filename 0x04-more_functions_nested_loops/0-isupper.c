#include "main.h"

/**
 * _isupper - Check if a letter is upper
 * @x: The number to be checked 
 * Return: give 1 or 0
 */
int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}

	return (0);
}
